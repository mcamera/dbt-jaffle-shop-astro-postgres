version: "3.1"
services:
  scheduler:
    volumes:
      - ./dags/dbt:/usr/local/airflow/dbt:rw

  webserver:
    volumes:
      - ./dags/dbt:/usr/local/airflow/dbt:rw

  triggerer:
    volumes:
      - ./dags/dbt:/usr/local/airflow/dbt:rw
